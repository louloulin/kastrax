jdk:
  - openjdk17

before_install:
  - echo "Preparing Kastrax build..."

install:
  - echo "Building Kastrax AI Framework..."
  - ./gradlew clean build publishToMavenLocal -x test

after_success:
  - echo "Build completed successfully!"

env:
  GRADLE_OPTS: "-Xmx2g -XX:MaxMetaspaceSize=512m"
