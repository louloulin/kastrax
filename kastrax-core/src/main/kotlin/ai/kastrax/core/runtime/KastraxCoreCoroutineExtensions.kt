package ai.kastrax.core.runtime

import ai.kastrax.runtime.coroutines.KastraxCoroutineGlobal
import ai.kastrax.runtime.coroutines.KastraxCoroutineScope
import ai.kastrax.runtime.coroutines.KastraxDeferred
import ai.kastrax.runtime.coroutines.KastraxDispatcher
import ai.kastrax.runtime.coroutines.KastraxJob
import ai.kastrax.runtime.coroutines.Default
import ai.kastrax.runtime.coroutines.IO
import ai.kastrax.runtime.coroutines.Main
import ai.kastrax.runtime.coroutines.createScope
import ai.kastrax.runtime.coroutines.getScope
import ai.kastrax.runtime.coroutines.launchKastrax
import ai.kastrax.runtime.coroutines.runBlockingKastrax
import ai.kastrax.runtime.coroutines.withCompute
import ai.kastrax.runtime.coroutines.withIO
import ai.kastrax.runtime.coroutines.withUI
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.Job
import kotlinx.coroutines.SupervisorJob
import kotlinx.coroutines.launch
import kotlinx.coroutines.runBlocking
import kotlinx.coroutines.withContext
import kotlin.coroutines.CoroutineContext
import kotlin.coroutines.EmptyCoroutineContext

// 使用ai.kastrax.runtime.coroutines中的withIO函数

// 使用ai.kastrax.runtime.coroutines中的withCompute函数

// 使用ai.kastrax.runtime.coroutines中的withUI函数

// 使用ai.kastrax.runtime.coroutines中的launch函数

// 使用ai.kastrax.runtime.coroutines中的runBlockingKastrax函数

// 使用ai.kastrax.runtime.coroutines中的getScope函数

// 使用ai.kastrax.runtime.coroutines中的createScope函数

// 使用ai.kastrax.runtime.coroutines中的IO调度器

// 使用ai.kastrax.runtime.coroutines中的Default调度器

// 使用ai.kastrax.runtime.coroutines中的Main调度器

// 使用ai.kastrax.runtime.coroutines中的launchKastrax函数

// 使用ai.kastrax.runtime.coroutines中的async函数
