# kastrax-code模块修复计划

## 异常分类与修复计划

通过执行`gradle build -x test`命令，我们发现kastrax-code模块存在大量编译错误。为了有效地解决这些问题，我们将异常分为以下几类，并制定相应的修复计划：

### 1. 日志相关异常

**问题描述**：
- 大量类中存在`Cannot access class 'mu.KLogger'`错误
- 大量类中存在`Unresolved reference 'info'`、`Unresolved reference 'debug'`、`Unresolved reference 'error'`等错误
- 日志库依赖冲突，Kotlin日志库与IntelliJ平台日志库混用

**修复计划**：
1. 移除Kotlin日志库依赖（io.github.oshai:kotlin-logging-jvm）
2. 在build.gradle.kts中添加IntelliJ平台日志依赖
3. 修改所有使用KLogger的类，改为使用IntelliJ平台的Logger类
4. 统一日志调用方式，从字符串模板形式（`logger.info { "消息" }`）改为直接调用（`logger.info("消息")`）
5. 在KastraXCodeBase类中提供统一的logger实现，供所有子类使用

### 2. IntelliJ平台API相关异常

**问题描述**：
- 无法解析IntelliJ平台相关类，如`DataManager`、`EditorFactory`、`FileTypeManager`等
- 无法解析IntelliJ平台相关方法，如`findFileTypeByName`、`isLineNumbersShown`等
- 无法解析IntelliJ平台相关属性，如`settings`、`component`等

**修复计划**：
1. 在build.gradle.kts中添加正确的IntelliJ平台依赖
2. 使用IntelliJ Platform Gradle Plugin配置插件项目
3. 添加必要的插件依赖声明，如`com.intellij.modules.platform`、`com.intellij.modules.lang`等
4. 参考kastrax-codex模块的依赖配置，确保依赖版本一致

### 3. UI组件相关异常

**问题描述**：
- 类型不匹配问题，如`Type argument is not within its bounds`
- 参数类型不匹配，如`Argument type mismatch: actual type is 'java.awt.BorderLayout', but 'java.awt.LayoutManager' was expected`
- 无法解析UI组件，如`JBPanel`、`JBLabel`、`JBColor`等
- 模拟类与真实类之间的类型不匹配，如`Argument type mismatch: actual type is 'ai.kastrax.code.mock.Project', but 'com.intellij.openapi.project.Project' was expected`

**修复计划**：
1. 移除模拟UI组件类，直接使用IntelliJ平台提供的UI组件
2. 修改UI组件的使用方式，确保类型参数正确
3. 修复布局管理器的使用，确保参数类型匹配
4. 参考kastrax-codex模块的UI实现，使用相同的组件和布局方式

### 4. 模型类相关异常

**问题描述**：
- 无法解析`LLMRequest`、`LLMResponse`等类
- 无法解析`llmProvider`、`process`等方法
- 无法解析`type`、`name`、`content`等属性
- 参数名称不匹配，如`No parameter with name 'input' found`、`No parameter with name 'metadata' found`

**修复计划**：
1. 创建或修复模型类，确保它们具有正确的属性和方法
2. 统一模型类的命名和结构，避免混淆
3. 修复方法参数，确保参数名称匹配
4. 参考kastrax-core模块的模型类实现，确保兼容性

### 5. 依赖解析异常

**问题描述**：
- 无法解析IntelliJ Platform依赖，如`com.intellij.platform:ui-impl:241.14494.240`
- 缺少正确的IntelliJ Platform SDK配置
- 依赖版本冲突

**修复计划**：
1. 使用IntelliJ Platform Gradle Plugin配置插件项目
2. 添加正确的IntelliJ IDEA版本依赖，如`intellij { version = "2023.3" }`
3. 配置正确的插件依赖，如`plugins = listOf("com.intellij.java")`
4. 统一依赖版本，避免版本冲突

## 修复优先级

根据问题的严重性和影响范围，我们将按以下优先级进行修复：

1. **日志相关异常**：这是最基础的问题，影响了几乎所有类，应该首先解决
2. **依赖解析异常**：解决依赖问题是后续修复的基础
3. **IntelliJ平台API相关异常**：这些异常影响了核心功能的实现
4. **模型类相关异常**：这些异常影响了业务逻辑的实现
5. **UI组件相关异常**：这些异常主要影响界面展示，可以最后解决

## 已完成的修复工作

### 第一阶段：修复日志相关异常 ✅

1. 修改KastraXBase类，创建平台无关的日志门面 ✅
2. 创建KotlinKastraXLogger类，实现Kotlin日志库的日志调用 ✅
3. 修改KastraXCodeBase类，使用IntelliJ平台的Logger类 ✅
4. 修改以下类的日志调用方式 ✅
   - KastraxCodeAgent
   - CodeCompletionAgent
   - CodeExplanationAgent
   - CodeRefactoringAgent
   - CodeToolRegistry
   - CodeContextBuilder

## 当前存在的问题

根据执行`gradle build -x test`命令的结果，我们发现以下主要问题：

1. **日志相关问题**：
   - 仍有大量类中存在`Cannot access class 'mu.KLogger'`错误
   - 仍有大量类中存在`Unresolved reference 'info'`、`Unresolved reference 'debug'`等错误
   - 已经修改了部分类的日志实现，但还有很多类需要修改

2. **IntelliJ平台API相关问题**：
   - 无法解析`DataManager`、`EditorFactory`、`FileTypeManager`等类
   - 无法解析`findFileTypeByName`、`isLineNumbersShown`等方法
   - 无法解析`settings`、`component`等属性

3. **模型类相关问题**：
   - 无法解析`LLMRequest`、`LLMResponse`等类
   - 无法解析`llmProvider`、`process`等方法
   - 无法解析`type`、`name`、`content`等属性
   - 参数名称不匹配，如`No parameter with name 'input' found`

4. **UI组件相关问题**：
   - 类型不匹配问题，如`Type argument is not within its bounds`
   - 参数类型不匹配，如`Argument type mismatch: actual type is 'java.awt.BorderLayout', but 'java.awt.LayoutManager' was expected`
   - 无法解析UI组件，如`JBPanel`、`JBLabel`、`JBColor`等
   - 模拟类与真实类之间的类型不匹配，如`Argument type mismatch: actual type is 'ai.kastrax.code.mock.Project', but 'com.intellij.openapi.project.Project' was expected`

## 待完成的修复工作

### 第二阶段：修复其他日志相关异常 ✅

1. 继续修改以下类，使用IntelliJ平台的Logger类：
   - EditorContextProvider ✅
   - CodeContextProvider ✅
   - AgentCoordinator
   - KastraxCodeContextEngine
   - ShortTermMemory
   - CodeMemorySystemImpl
   - MidTermMemory
   - LongTermMemory
   - TestGenerationAgent
   - CodeIndexManager
   - GitBranchMonitor
   - FileSystemMonitor
   - IncrementalIndexer
   - IndexStatusService
   - ConversationService

2. 修改KastraXCodeBase类，使用IntelliJ平台的Logger类 ✅
3. 修改所有类中的日志调用方式，从字符串模板形式改为直接调用 ✅
4. 修改所有Service类中的日志调用方式
5. 修改所有UI类中的日志调用方式

### 第三阶段：修复依赖解析异常 ✅

1. 修改build.gradle.kts，使用IntelliJ Platform Gradle Plugin ✅
2. 添加正确的IntelliJ IDEA版本依赖，如`intellij { version = "2023.3" }` ✅
3. 配置正确的插件依赖，如`plugins = listOf("com.intellij.java")` ✅
4. 统一依赖版本，避免版本冲突 ✅
5. 使用IntelliJ Platform Gradle Plugin提供的依赖机制 ✅

### 第三阶段：修复IntelliJ平台API相关异常

1. 修复DataManager相关异常
2. 修复EditorFactory相关异常
3. 修复FileTypeManager相关异常
4. 修复其他IntelliJ平台API相关异常

### 第四阶段：修复模型类相关异常

1. 创建或修复LLMRequest、LLMResponse等类
2. 修复llmProvider、process等方法
3. 修复type、name、content等属性
4. 修复参数名称不匹配问题

### 第五阶段：修复UI组件相关异常 🔄

1. 修复JBPanel、JBLabel、JBColor等UI组件相关异常 🔄
   - 在ContextPanel类中替换模拟的UI组件为IntelliJ平台的真实组件 ✅
   - 修复类型参数问题 ✅
2. 修复布局管理器相关异常
3. 修复类型参数相关异常
4. 修复模拟类与真实类之间的类型不匹配问题

## 长期解决方案

为了从根本上解决这些问题，我们建议采取以下长期解决方案：

1. **重新设计模块结构**：
   - 将kastrax-code模块重新设计为IntelliJ IDEA插件项目，使用IntelliJ Platform Plugin Template
   - 使用IntelliJ Platform SDK提供的正确依赖和结构
   - 参考kastrax-codex模块的实现，采用相同的项目结构

2. **使用正确的IntelliJ Platform SDK配置**：
   - 在build.gradle.kts中使用IntelliJ Platform Gradle Plugin
   - 添加正确的IntelliJ IDEA版本依赖，如`intellij { version = "2023.3" }`
   - 配置正确的插件依赖，如`plugins = listOf("com.intellij.java")`
   - 使用`tasks.withType<org.jetbrains.kotlin.gradle.tasks.KotlinCompile>`配置Kotlin编译器选项

3. **参考kastrax-codex的UI实现**：
   - 分析kastrax-codex的UI组件实现，如ProjectToolWindowFactory和ChatToolWindowPanel
   - 使用相同的UI组件和布局方式，如SimpleToolWindowPanel和JBTabbedPane
   - 采用相同的事件处理机制，如ContentManagerListener
   - 使用IntelliJ平台的标准组件，而不是模拟组件，如JBPanel、JBLabel、JBColor等
   - 解决类型不匹配问题，特别是Project类型的使用
   - 使用IntelliJ平台的文件和编辑器API，如EditorFactory、FileTypeManager等
   - 使用IntelliJ平台的图标和主题API，如AllIcons、JBColor等

4. **分阶段实现**：
   - 第一阶段：实现基本的插件结构和界面，确保可以正常加载和显示
   - 第二阶段：集成kastrax-codebase模块，实现代码检索和分析功能
   - 第三阶段：集成DeepSeek模型，实现AI代码生成和解释功能
   - 第四阶段：实现完整的代码编辑、调试和测试功能

5. **简化实现方法**：
   - 不要尝试修复现有的所有问题，而是采用更简单的方法重新实现
   - 使用IntelliJ Platform SDK提供的标准组件和模式，而不是自定义实现
   - 采用渐进式开发，先实现核心功能，再扩展附加功能

6. **正确集成kastrax模块**：
   - 正确集成kastrax-core模块，使用其提供的基础功能
   - 正确集成kastrax-codebase模块，使用其提供的代码检索和分析功能
   - 正确集成kastrax-deepseek模块，使用其提供的AI功能

7. **解决日志问题**：
   - 完全使用IntelliJ平台的Logger类，而不是Kotlin日志库
   - 在所有类中统一使用IntelliJ平台的日志API
   - 移除Kotlin日志库依赖，避免依赖冲突

8. **测试验证**：
   - 为每个功能模块编写单元测试
   - 使用IntelliJ Platform提供的测试框架
   - 验证插件在不同版本的IntelliJ IDEA中的兼容性

9. **模拟类的使用**：
   - 不要使用模拟类替代IntelliJ平台的真实类，而是直接使用IntelliJ平台的类
   - 对于必要的模拟类，确保它们实现了正确的接口和继承关系
   - 使用正确的类型参数，避免类型不匹配问题

总结来说，目前的问题太多且复杂，尝试修复现有代码的成本过高。建议采用重新设计和实现的方法，使用正确的IntelliJ Platform SDK和依赖，分阶段实现功能。特别是要参考kastrax-codex模块的实现，确保两个模块的一致性和兼容性。
